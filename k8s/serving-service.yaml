apiVersion: v1
kind: Service
metadata:
  name: pytorch-serving-service
  namespace: mlops-pipeline
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: inference-server
