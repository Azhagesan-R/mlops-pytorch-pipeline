apiVersion: v1
kind: ConfigMap
metadata:
  name: training-config
  namespace: mlops-pipeline
data:
  training_config.yaml: |
    model:
      num_classes: 10
    training:
      epochs: 5
      batch_size: 16
      lr: 0.001
      num_samples: 160
